# 決算ステータス｜決算をゲームのステータスで見せる

**このリポジトリで作業するときは、まずこの1枚を読む。**

> **決算を読めない人にも、会社の状態が一目で分かるようにする。**

名前は仮。2026-09-13に giraffess リポジトリ（旧 `docs/35`）から分離した。**四季報のように、上場企業のページを増やしていく。**

**公開中：https://hiroho-t.github.io/kessan-status/**（2026-09-13〜）
- リポジトリは public（無料プランで GitHub Pages を使うため。`docs/` と `log/` も誰でも読める）
- サイトに出るのは `pages/` だけ（`.github/workflows/pages.yml`）。`pages/` を push すると自動で反映される
- **公開されている前提で書く。**社外に出せない数字・値付け・取引先名、**EDINET API のキー**は、このリポジトリに書かない

---

## 0. 起動時に読む順番

| 順 | ファイル | なぜ先か |
|---|---|---|
| 1 | **このファイル** | 越えない線と、作り直しの手順 |
| 2 | [docs/01_事業の定義.md](docs/01_事業の定義.md) **正本** | キーニーズ法／見せ方の案／土台（EDINET）／既存／法務の線／ビジネスモデル候補／実証実験の案 |

**読まずに着手しない。**

---

## 1. 越えない線

| # | 線 |
|---|---|
| 1 | **「強い＝買い」「弱い＝避けろ」と書かない。**おすすめ銘柄を出さない（投資助言の線。`docs/01` section 7） |
| 2 | **会員限定・有料で見せる形にしない**（投資助言・代理業の登録が要る形になる） |
| 3 | **数字は決算短信・有価証券報告書の原本から取る。**まとめサイトの数字は使わない（個別と連結の取り違えが実際にあった） |
| 4 | **連結の数字を使う。**連結が無い会社だけ個別 |
| 5 | **ポケモンなど他社の名前・絵・言葉を使わない** |
| 6 | **「レベル」は規模だけを表す。「規模Lv.」と書き、経営の良し悪しに見せない** |
| 7 | **式は仮。**会計の専門家に確認するまで「仮」と書く。**式を変えたら `about.html`（数字の考え方）の文章も同時に直す** |
| 8 | **金融の4業種（銀行・証券・保険・その他金融）は載せない。**式が合わない |

---

## 2. サイトの形

| ページ | ファイル | 中身 |
|---|---|---|
| トップ | `pages/index.html` | 検索（社名・よみがな・証券コード・英字名）／業界一覧（東証33業種から金融4業種を除いた29） |
| 業界別一覧 | `pages/industry/<業種>.html` | **規模Lvが高い順**（同じLvは売上高が大きい順） |
| 会社 | `pages/company/<証券コード>.html` | ステータス・状態異常・ダメージ・次の期の見通し・出典 |
| 数字の考え方 | `pages/about.html` | 式の解説 |
| 共通 | `pages/style.css`／`pages/search-index.js` | 320px〜1920px以上で崩れないこと。フッター＝業界一覧・数字の考え方・運用会社（startwith.org）・© StartWith |

**`pages/` は手で直さない。**必ず `tools/make_status.py` で作り直す（ページの文章・CSS・式もこの中にある）。

---

## 3. 作業→場所

| 依頼 | どこ |
|---|---|
| 事業の定義・法務・ビジネスモデル | `docs/01_事業の定義.md` **正本** |
| **会社を増やす（自動）** | `tools/fetch_edinet.py`（EDINET API。有価証券報告書のXBRLから取る） |
| 会社を足す・数字を直す（手） | `data/companies/<証券コード>.json`（金額は百万円） |
| ステータスの式・画面・文章 | `tools/make_status.py` |
| 作業の日記 | `log/YYYY-MM-DD_短い題.md` |
| 会社を正しく知るシリーズ全体 | **giraffess リポジトリ** `docs/36_会社を正しく知るシリーズ.md` |

### 会社を増やす

```bash
# 準備（1回だけ）：EDINET API のキーを置く。リポジトリには入れない
mkdir -p ~/.config/kessan-status && pbpaste > ~/.config/kessan-status/edinet_api_key

python3 tools/fetch_edinet.py --industry 輸送用機器   # 業種ごと
python3 tools/fetch_edinet.py --codes 6758 9984       # 証券コードを指定
python3 tools/fetch_edinet.py --all --limit 200       # 証券コード順に200社
python3 tools/make_status.py                          # ページを作り直す
```

- 有価証券報告書から取れるのは実績だけ。**次の期の見通し・今期受けたダメージは、決算短信を読んで手で入れる**（`forecast`・`damages`）
- 決算短信から手で入れた会社は、同じ期なら fetch で上書きされない
- 取れなかった会社は `data/cache/skipped.tsv` に理由つきで出る。要素IDが見つからないときは `fetch_edinet.py` の候補リストに足す
- 書類一覧とXBRLは `data/cache/` に貯まる（git に入れない）

### ドライブへの同期

```bash
rsync -a --delete pages/ "$HOME/Library/CloudStorage/GoogleDrive-hiroho.tsukiji@gmail.com/マイドライブ/kessan-status/pages/"
```

---

## 4. いま何をしているか（2026-09-13）

| 順 | やること | 状態 |
|---|---|---|
| 1 | 1社で試す（トヨタ） | 済 |
| 2 | 同業種で並べる（自動車5社、2026年3月期・決算短信から手入力） | 済 |
| 3 | 検索・業界別一覧・数字の考え方・レスポンシブ | 済 |
| 4 | **EDINET API のキーを取る（社長）** → `fetch_edinet.py` を実データで試す | **未。スクリプトは作り込み済み・実データでは未検証** |
| 5 | 式の見直し（**HPは現金を貯めこむ会社ほど高く出る。**スズキが最下位になった） | 未 |
| 6 | 会計の専門家に式を見てもらう | 未 |

---

## 5. 書き方

日本語の文章は `writing-rules` と `concrete-writing` に従う。画面はモノトーン・Noto Sans JP・装飾なし。アイコンは Font Awesome 無料版だけ。
